# Proxy 安全策略

## 仓库定位

Proxy 是公开仓库，只发布已经脱敏、可长期公开访问的静态资产。RelayDeck 负责敏感来源、完整配置、节点订阅和动态发布。

## 禁止提交

严禁提交以下内容：

- 节点地址、分享链接、订阅 URL、面板地址或真实私密 URL
- 密码、令牌、API Key、私钥、证书、Cookie、认证请求头
- VPS IP、个人服务域名、设备标识、用户资料或运行日志
- 包含上述内容的完整客户端配置、备份和测试快照

## 发布前要求

提交前必须完成脱敏、客户端语法校验、跨文件引用校验和敏感信息扫描。资产清单中的 `public: true` 只表示该静态文件已获准公开，不代表 RelayDeck 生成的完整配置可以公开。

## 暴露事件

如发现敏感内容已经进入工作区或 Git 历史：

1. 立即停止同步和发布，不要继续扩散链接。
2. 立即在来源系统撤销或轮换受影响的凭据。
3. 清理公开分支、Release、Issue 和缓存中的暴露内容，并评估 Git 历史清理需求。
4. 在 RelayDeck 中回退到最近一次通过校验的活动版本。

仅删除当前文件不能使已暴露凭据失效，也不能自动清除 Git 历史中的内容。

## RelayDeck 约束

RelayDeck 必须拒绝未登记、哈希不符、客户端不匹配或 `public: false` 的资产；GitHub 不可用时继续服务最近一次有效活动版本，不能发布空配置。
